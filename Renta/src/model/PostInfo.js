class PostInfo{

  getPostInfo(info){
    let postInfo = new PostInfo();
    postInfo.firstName = info.firstName;
    postInfo.lastName = info.lastName;
    postInfo.email = info.email;
    return user;
  }
}

export default UserInfo;